spring:
  application:
    name: user-service
  config:
    import: "optional:configserver:http://config-server:8888"
  cloud:
    config:
      #uri: http://config-server:8888
      name: user-service
      profile: default
      fail-fast: true

server: 
  port: 8080 