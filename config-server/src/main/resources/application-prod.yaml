server:
  port: 8080
spring:
#  profiles:
#    active: git
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${GIT_CONFIG_REPO}
          default-label: ${GIT_CONFIG_BRANCH}
          search-paths:
            - config
           # - '{application}'
          clone-on-start: true # clone the repo when server start
eureka:
  client:
    service-url:
      defaultZone: http://eureka-service:8761/eureka